{
  "entities": {
    "Feedback": {
      "title": "User Feedback",
      "description": "Suggestions and bug reports from users",
      "type": "object",
      "properties": {
        "message": {
          "type": "string",
          "description": "The feedback message content"
        },
        "type": {
          "type": "string",
          "enum": ["Suggestion", "Bug"],
          "description": "The category of feedback"
        },
        "name": {
          "type": "string",
          "description": "The name of the user (optional)"
        },
        "email": {
          "type": "string",
          "description": "The email of the user (optional)"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the feedback was submitted"
        }
      },
      "required": ["message", "type", "createdAt"]
    }
  },
  "firestore": {
    "/feedback/{feedbackId}": {
      "schema": "Feedback",
      "description": "Collection of all user feedback"
    }
  }
}
